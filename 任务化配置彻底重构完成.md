# 任务化配置彻底重构完成

## 重构时间
2026-01-21

## 重构目标
彻底移除旧的配置兼容性，实现完全的任务化配置，配置结构更清晰、更易维护。

## 重构内容

### 1. 配置文件重组

#### 删除的旧配置文件
- `config/audit_tasks.yaml` - 旧的操作日志表配置
- `config/audit_tasks_test.yaml` - 旧的测试配置

#### 新的配置文件结构
```
config/tasks/
├── operation_log.yaml          # 操作日志表任务配置（43个任务）
├── gold_log.yaml               # 金库日志表任务配置（20个字段）
└── special_statistics.yaml     # 特殊统计任务配置（1个任务）
```

### 2. SQL模板重组

#### 新的SQL模板目录结构
```
sql_templates/
├── operation_log/              # 操作日志表SQL模板（20个文件）
│   ├── operation_log_null_check_stats.sql
│   ├── operation_log_null_check_sample.sql
│   ├── operation_log_time_null_check_stats.sql
│   ├── operation_log_time_null_check_sample.sql
│   ├── operation_log_conditional_null_check_stats.sql
│   ├── operation_log_conditional_null_check_sample.sql
│   ├── operation_log_uniqueness_check_stats.sql
│   ├── operation_log_uniqueness_check_sample.sql
│   ├── operation_log_enum_check_stats.sql
│   ├── operation_log_enum_check_sample.sql
│   ├── operation_log_semantic_check_stats.sql
│   ├── operation_log_semantic_check_sample.sql
│   ├── operation_log_correctness_check_stats.sql
│   ├── operation_log_correctness_check_sample.sql
│   ├── operation_log_locked_accounts_stats.sql
│   ├── operation_log_locked_accounts_sample.sql
│   ├── operation_log_statistical_analysis_stats.sql
│   ├── operation_log_statistical_analysis_sample.sql
│   ├── operation_log_exec_result_anomaly_check_stats.sql
│   └── operation_log_exec_result_anomaly_check_sample.sql
├── gold_log/                   # 金库日志表SQL模板（15个文件）
│   └── gold_log_*.sql
└── special/                    # 特殊任务SQL模板（2个文件）
    ├── special_table_count_stats.sql
    └── special_detail_query.sql
```

#### 删除的旧SQL模板
所有根目录下的通用SQL模板已删除，全部移动到对应的表目录中并添加前缀。

### 3. 操作日志表任务配置（43个任务）

#### 基础字段检查（39个任务）
1. log_id - 唯一性检查
2. log_id - 非空检查
3. session_id - 非空检查
4. log_source - 枚举值检查
5. log_source - 非空检查
6. operate_source - 枚举值检查
7. operate_source - 非空检查
8. natural_org_path - 非空检查
9. natural_name - 非空检查
10. natural_status - 非空检查
11. natural_status - 枚举值检查
12. account_id - 非空检查
13. account_name - 非空检查
14. account_status - 枚举值检查
15. account_status - 非空检查
16. account_role - 枚举值检查
17. account_role - 非空检查
18. person_liable - 条件非空检查
19. from_account - 条件非空检查
20. from_account_status - 枚举值检查
21. from_account_status - 条件非空检查
22. from_account_type - 枚举值检查
23. from_account_type - 条件非空检查
24. custom_ip - 非空检查
25. client - 语义检查
26. operate_time - 时间非空检查
27. collect_time - 时间非空检查
28. server_ip - 非空检查
29. dst_device_type - 枚举值检查
30. dst_device_type - 非空检查
31. protocol - 条件非空检查
32. operate_name - 语义检查
33. operate_command - 非空检查
34. operate_command - 语义检查
35. operate_content - 非空检查
36. operate_content - 语义检查
37. operate_type - 非空检查
38. exec_result - 枚举值检查
39. exec_result - 非空检查

#### 新增的特殊任务（4个任务）
40. exec_result - 异常占比检查（检查除"成功"和"失败"外的异常值）
41. database_service - 正确性检查
42. from_account_status - 账号加锁统计
43. gold_grant_result - 统计分析

### 4. 代码清理

#### config_manager.py 清理
- 删除 `load_audit_tasks()` 方法（旧配置加载方法）
- 简化 `load_table_config()` 方法，只从 `config/tasks/` 读取
- 简化 `list_all_tables()` 方法，只从 `config/tasks/` 读取
- 移除所有旧配置兼容代码

#### 配置验证字段简化
- 移除 `standard_table`, `error_table`, `time_field` 的必需验证
- 只保留 `name` 和 `template_dir` 作为必需字段
- 特殊任务配置（如 special_statistics）不需要表名配置

### 5. 新增SQL模板文件

为了完整支持老配置中的所有检查类型，新增了以下SQL模板：

1. **operation_log_locked_accounts_stats.sql** - 账号加锁统计（统计查询）
2. **operation_log_exec_result_anomaly_check_stats.sql** - exec_result异常占比核查（统计查询）
3. **operation_log_exec_result_anomaly_check_sample.sql** - exec_result异常占比核查（样例查询）
4. **operation_log_correctness_check_sample.sql** - 正确性核查（样例查询）

## 配置格式说明

### 任务化配置格式
```yaml
table_info:
  name: "operation_log"
  display_name: "4A操作日志表"
  description: "4A系统操作日志数据质量稽核"
  standard_table: "argus.bg_4a_operation_log_standard"  # 可选
  error_table: "argus.bg_4a_operation_log_error"        # 可选
  time_field: "generic_into_time"                       # 可选
  primary_key: "log_id"                                 # 可选
  template_dir: "operation_log"                         # 必需

tasks:
  - task_id: "log_id_uniqueness"
    task_name: "日志ID唯一性检查"
    enabled: true
    sql_template: "operation_log_uniqueness_check"
    params:
      field: "log_id"
      display_name: "日志ID"
```

## 使用方式

### 1. 执行单表稽核
```bash
# 执行操作日志表稽核
python main.py --table operation_log

# 执行金库日志表稽核
python main.py --table gold_log

# 执行特殊统计任务
python main.py --table special_statistics
```

### 2. 执行所有表稽核
```bash
python main.py --all-tables
```

### 3. 生成大SQL文件
```bash
# 生成所有表的大SQL
python main.py --gen-sql

# 生成指定表的大SQL
python main.py --gen-sql --table operation_log
```

## 优势

1. **配置清晰** - 每个表一个配置文件，任务化配置更直观
2. **易于维护** - SQL模板按表分目录，文件名带前缀，不会混淆
3. **灵活扩展** - 添加新表只需创建新的配置文件和SQL模板目录
4. **无历史包袱** - 完全移除旧配置兼容代码，代码更简洁
5. **任务粒度** - 每个任务独立配置，可以单独启用/禁用

## 测试建议

1. 测试操作日志表稽核（43个任务）
2. 测试金库日志表稽核（20个字段）
3. 测试特殊统计任务
4. 测试生成大SQL功能
5. 验证所有SQL模板文件是否正确

## 注意事项

1. 旧的 `config/audit_tasks.yaml` 已删除，不再支持
2. SQL模板必须放在对应的表目录中，并添加表名前缀
3. 配置文件必须放在 `config/tasks/` 目录中
4. 特殊任务配置不需要 `standard_table` 等字段
