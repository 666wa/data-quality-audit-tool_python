# 任务化配置重构说明

## 改动概述

将稽核配置从字段+检查类型的方式改为任务化配置，每个任务独立配置，更加清晰和灵活。

---

## 配置格式变化

### 旧格式
```yaml
audit_tasks:
  - field: gold_grant_result
    display_name: "金库授权结果"
    checks:
      - uniqueness_check
      - null_check

special_tasks:
  - name: table_count_statistics
    type: table_count
```

### 新格式
```yaml
tasks:
  - task_id: "gold_grant_result_uniqueness"
    task_name: "金库授权结果唯一性检查"
    enabled: true
    sql_template: "gold_log_uniqueness_check"
    params:
      field: "gold_grant_result"
      display_name: "金库授权结果"
  
  - task_id: "gold_grant_result_null"
    task_name: "金库授权结果非空检查"
    enabled: true
    sql_template: "gold_log_null_check"
    params:
      field: "gold_grant_result"
      display_name: "金库授权结果"
```

---

## SQL模板命名变化

### 旧命名
```
sql_templates/gold_log/
├── null_check_stats.sql
├── null_check_sample.sql
├── uniqueness_check_stats.sql
└── uniqueness_check_sample.sql
```

### 新命名（已完成）
```
sql_templates/gold_log/
├── gold_log_null_check_stats.sql
├── gold_log_null_check_sample.sql
├── gold_log_uniqueness_check_stats.sql
└── gold_log_uniqueness_check_sample.sql
```

---

## 代码改动点

### 1. config_manager.py
- `load_table_config()` - 兼容新旧两种配置格式
- 新增 `list_tasks()` - 列出所有任务
- 新增 `get_task()` - 获取指定任务

### 2. audit_executor.py
- 修改 `execute_field_audit()` 改为 `execute_task()`
- 修改 `execute_all_audits()` 支持任务列表
- 修改模板加载逻辑，使用 `sql_template` 字段

### 3. main.py
- 新增 `--task` 参数 - 指定运行的任务ID
- 新增 `--list-tasks` 参数 - 列出所有任务
- 修改稽核执行逻辑

### 4. report_generator.py
- 修改报告生成逻辑，使用任务ID和任务名称

---

## 使用方式变化

### 旧方式
```bash
python main.py --table gold_log
```

### 新方式
```bash
# 运行所有任务
python main.py --table gold_log

# 运行指定任务
python main.py --table gold_log --task gold_grant_result_null

# 运行多个任务
python main.py --table gold_log --task gold_grant_result_null,resource_pool_null

# 列出所有任务
python main.py --table gold_log --list-tasks
```

---

## 兼容性

- ✅ 兼容旧的配置格式（audit_tasks + special_tasks）
- ✅ 兼容旧的SQL模板命名（无前缀）
- ✅ 新旧配置可以共存

---

## 实施步骤

1. ✅ 重命名SQL模板文件（添加表名前缀）
2. ✅ 创建新的配置文件格式
3. ⏳ 修改 config_manager.py
4. ⏳ 修改 audit_executor.py
5. ⏳ 修改 main.py
6. ⏳ 修改 report_generator.py
7. ⏳ 测试验证
8. ⏳ 更新文档

---

## 优势

1. **更清晰**：每个任务独立配置，一目了然
2. **更灵活**：可以单独启用/禁用任务
3. **更易维护**：任务ID唯一标识，便于追踪
4. **更易扩展**：添加新任务只需添加一个配置块
5. **更易调试**：可以单独运行某个任务进行调试
