# 任务目录配置完成

## 实施时间
2026-01-21 19:26

## 改动内容

### ✅ 已完成

#### 1. 创建任务配置目录
```
config/tasks/          # 新的任务配置目录
├── gold_log.yaml      # 金库日志表配置（26个任务）
└── gold_log_test.yaml # 金库日志表测试配置（3个任务）
```

#### 2. 更新配置文件
**config/config.yaml** 和 **config/config.example.yaml**：
```yaml
# 目录配置
tasks_dir: "./config/tasks"  # 任务配置目录（默认: ./config/tasks）
```

#### 3. 修改代码
**src/config_manager.py**：
- 新增 `get_tasks_dir()` 方法 - 获取任务配置目录
- 修改 `load_table_config()` - 优先从 tasks 目录读取
- 修改 `list_all_tables()` - 从 tasks 目录读取所有任务

#### 4. 测试验证
- ✅ 单表稽核：`python main.py --table gold_log_test`
- ✅ 所有表稽核：`python main.py --all-tables`
- ✅ 找到2个表配置：gold_log, gold_log_test

---

## 配置优先级

系统按以下顺序查找任务配置：

1. **config/tasks/表名.yaml**（新方式，优先）✅
2. config/表名.yaml（兼容）
3. config/tables/表名.yaml（兼容旧方式）

---

## 目录结构

### 新结构（推荐）
```
config/
├── config.yaml              # 主配置（数据库、时间范围、目录配置）
├── config.example.yaml      # 配置示例
├── audit_tasks.yaml         # 旧的操作日志表配置（兼容）
└── tasks/                   # 任务配置目录 ✨
    ├── gold_log.yaml        # 金库日志表任务配置
    └── gold_log_test.yaml   # 金库日志表测试配置
```

### 优势
- ✅ **更清晰**：任务配置集中在 tasks 目录
- ✅ **更灵活**：可在 config.yaml 中配置目录路径
- ✅ **更易管理**：所有任务配置在一个目录
- ✅ **向后兼容**：仍支持旧的配置位置

---

## 使用方式

### 1. 稽核单个表
```bash
python main.py --table gold_log_test
```

### 2. 稽核所有表
```bash
python main.py --all-tables
```
系统会自动读取 `config/tasks/` 目录下的所有 `.yaml` 文件。

### 3. 自定义任务目录
编辑 `config/config.yaml`：
```yaml
tasks_dir: "./my_custom_tasks"  # 自定义任务目录
```

---

## 添加新任务

### 步骤1：创建任务配置文件
```bash
# 复制现有配置作为模板
cp config/tasks/gold_log_test.yaml config/tasks/new_table.yaml
```

### 步骤2：编辑配置文件
修改 `config/tasks/new_table.yaml`：
- 修改 `table_info` 中的表名、字段等
- 修改 `tasks` 中的任务配置

### 步骤3：创建SQL模板
```bash
mkdir sql_templates/new_table
cp sql_templates/gold_log/*.sql sql_templates/new_table/
# 重命名SQL模板文件，添加表名前缀
```

### 步骤4：执行稽核
```bash
python main.py --table new_table
```

---

## 配置示例

### config/config.yaml
```yaml
# 数据库连接
database_host: "10.10.26.93"
database_port: 8123
database_name: "argus"
database_user: "default"
database_password: "your_password"

# 时间范围
start_time: "2026-01-12 00:00:00"
end_time: "2026-01-19 00:00:00"

# 目录配置
tasks_dir: "./config/tasks"  # 任务配置目录
```

### config/tasks/gold_log_test.yaml
```yaml
table_info:
  name: "gold_log_test"
  display_name: "4A金库审批日志表（测试）"
  standard_table: "argus.bg_4a_gold_log_standard"
  error_table: "argus.bg_4a_gold_log_error"
  time_field: "generic_into_time"
  template_dir: "gold_log"

tasks:
  - task_id: "gold_grant_result_null"
    task_name: "金库授权结果非空检查"
    enabled: true
    sql_template: "gold_log_null_check"
    params:
      field: "gold_grant_result"
      display_name: "金库授权结果"
```

---

## 测试结果

### 测试1：单表稽核
```bash
python main.py --table gold_log_test
```
- ✅ 成功从 `config/tasks/gold_log_test.yaml` 读取配置
- ✅ 3个任务全部执行成功
- ✅ 报告生成成功

### 测试2：所有表稽核
```bash
python main.py --all-tables
```
- ✅ 找到2个表配置：gold_log, gold_log_test
- ✅ 开始稽核2个表
- ✅ 从 `config/tasks/` 目录读取配置

---

## 总结

✅ **任务目录配置已成功实施**

- 任务配置集中在 `config/tasks/` 目录
- 目录路径可在 `config.yaml` 中配置
- 向后兼容旧的配置位置
- 测试通过，可以投入使用

**推荐将所有任务配置文件放在 `config/tasks/` 目录！**
