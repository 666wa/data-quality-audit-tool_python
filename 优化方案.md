# 数据质量稽核工具优化方案

> 日期: 2026-05-27
> 适用环境: 服务器纯命令行环境（无GUI/Web）

---

## 一、CLI 交互体验增强

### 1.1 交互式任务选择器
**现状**: 用户需要硬记任务名 `python main.py --task operation_log`

**优化**: 添加交互式选择，支持模糊搜索

```bash
# 不带参数时，弹出交互式菜单
$ python main.py
? 选择要执行的稽核任务 (Use arrow keys)
  > 1. operation_log    - 4A操作日志表稽核 (12个子任务)
    2. gold_log         - 金库日志表稽核 (8个子任务)
    3. special_statistics - 特殊统计稽核 (3个子任务)

# 支持按编号或名称
$ python main.py --task 1
$ python main.py --task op          # 模糊匹配 operation_log
```

### 1.2 任务预览模式 (--preview)
**现状**: 执行任务前不知道会查什么、查多久

**优化**: 添加预览模式，展示任务概览

```bash
$ python main.py --task operation_log --preview
+-------------------------------------------------------------+
|  稽核任务预览: operation_log (4A操作日志表稽核)             |
+-------------------------------------------------------------+
|  时间范围: 2025-01-01 00:00:00 ~ 2025-01-01 23:59:59      |
|  目标表:  argus.bg_4a_operation_log_standard                  |
|          argus.bg_4a_operation_log_error                     |
+-------------------------------------------------------------+
|  即将执行 12 个子任务:                                      |
|    1. log_id_uniqueness      日志ID唯一性检查     [预计 2s] |
|    2. log_id_null            日志ID非空检查       [预计 1s] |
|    3. session_id_null        会话ID非空检查       [预计 1s] |
|    ...                                                       |
+-------------------------------------------------------------+
|  预估总耗时: ~15秒  确认执行? [Y/n]                          |
+-------------------------------------------------------------+
```

### 1.3 进度实时显示
**现状**: 执行过程中只有日志，不知道整体进度

**优化**: 添加进度条和实时状态

```
执行稽核任务: operation_log
[████████████████████░░░░░░░░░░░░] 12/20 (60%)  耗时: 8.5s
  [OK] log_id_uniqueness      12条异常  [0.8s]
  [OK] log_id_null            无异常   [0.6s]
  [OK] session_id_null        无异常   [0.7s]
  [->] src_ip_null            执行中... [1.2s]
```

### 1.4 快捷命令封装
**优化**: 提供常用操作的快捷脚本

```bash
# 生成快捷命令脚本
$ python main.py --install-shell-completion

# 之后可直接使用
$ audit --list                      # 列出所有任务
$ audit run operation_log           # 执行任务
$ audit preview operation_log       # 预览任务
$ audit sql operation_log           # 生成SQL文件
$ audit validate-config             # 校验配置
$ audit diff last current           # 对比两次稽核结果
```

---

## 二、配置管理体验优化

### 2.1 配置生成向导
**现状**: 新建任务需要手动复制YAML，容易出错

**优化**: 一键生成任务配置模板

```bash
$ python main.py --init-task my_new_task
? 任务显示名称: 新日志表稽核
? 标准表名: argus.bg_new_log_standard
? 错误表名: argus.bg_new_log_error
? 时间字段: generic_into_time
? 需要哪些检查类型? (Space选择, Enter确认)
  [X] 非空检查
  [X] 唯一性检查
  [ ] 枚举值检查
  [X] 条件非空检查

[OK] 已生成配置: config/tasks/my_new_task.yaml
[OK] 已生成SQL模板: sql_templates/my_new_task/
```

### 2.2 配置继承与复用
**现状**: 多个表的时间范围、数据库连接等重复配置

**优化**: 支持配置继承

```yaml
# config/tasks/base.yaml - 基础配置
_base: &base
  database_host: "10.10.26.93"
  start_time: "2026-01-01 00:00:00"
  end_time: "2026-01-02 00:00:00"

# config/tasks/operation_log.yaml
extends: base  # 继承基础配置，只需覆盖差异项
table_info:
  standard_table: "argus.bg_4a_operation_log_standard"
```

### 2.3 配置验证与诊断
**现状**: 配置错误只能在运行时暴露

**优化**: 提供专门的校验命令

```bash
$ python main.py --validate
[OK] config.yaml 校验通过
[OK] tasks/operation_log.yaml 校验通过
[OK] tasks/gold_log.yaml 校验通过
[ERR] tasks/special_statistics.yaml 配置有误:
    -> 第15行: sql_template "xxx_check" 对应的模板文件不存在
    -> 建议: 创建 sql_templates/special/xxx_check_stats.sql
```

---

## 三、纯文本报告可读性优化

### 3.1 ANSI 颜色高亮
**优化**: 报告和日志支持颜色区分

```
# 统计结果 - 异常用红色高亮
[ERROR] account_id_null      异常记录: 1,234,567 条 (15.3%)
[INFO]  log_id_uniqueness    无异常

# 汇总 - 通过率用绿色
[INFO] 稽核完成: 12/12 成功, 通过率: 100%
```

### 3.2 结构化文本表格
**优化**: 改善 report_generator.py 的表格格式化

```
+---------------------+----------+----------+----------+
| 设备类型            | 总记录数 | 异常记录 | 异常比例 |
+---------------------+----------+----------+----------+
| Windows_Server      |  523,456 |   12,345 |   2.36%  |
| Linux_CentOS        |  412,333 |      567 |   0.14%  |
| Firewall_Huawei     |  123,456 |   45,678 |  37.01%  | <- 重点关注
+---------------------+----------+----------+----------+
```

### 3.3 异常摘要汇总
**优化**: 报告顶部增加异常汇总，一眼看到问题

```
=======================================================
                    稽核异常摘要
=======================================================
发现异常的检查项 (3/12):
  [!] account_id_null      1,234,567 条异常  [查看详情 -> 第45行]
  [!] src_ip_null          45,678 条异常    [查看详情 -> 第67行]
  [!] dst_ip_enum          12,345 条异常    [查看详情 -> 第89行]

所有检查项均正常的 (9/12): log_id_uniqueness, log_id_null, ...
=======================================================
```

---

## 四、任务管理效率优化

### 4.1 任务分组与标签
**优化**: 支持按标签筛选执行

```yaml
# operation_log.yaml
tags: ["每日例行", "核心表"]
```

```bash
$ python main.py --tag "每日例行"        # 执行所有标记为"每日例行"的任务
$ python main.py --tag "核心表" --all-tasks  # 执行所有核心表
```

### 4.2 任务依赖与顺序
**优化**: 支持任务间依赖，前置失败自动跳过

```yaml
tasks:
  - task_id: "base_data_check"
    depends_on: []  # 无依赖

  - task_id: "advanced_check"
    depends_on: ["base_data_check"]  # 基础检查通过后才执行
```

### 4.3 任务模板复用
**优化**: 常用检查配置为模板，多处引用

```yaml
# 定义检查模板
_template_null_check: &null_check
  sql_template: "operation_log_null_check"
  enabled: true

# 多处复用
tasks:
  - task_id: "log_id_null"
    <<: *null_check
    params: { field: "log_id" }

  - task_id: "account_id_null"
    <<: *null_check
    params: { field: "account_id" }
```

---

## 五、运行体验优化

### 5.1 并发执行
**优化**: 独立任务并行执行，减少等待时间

```bash
$ python main.py --task operation_log --parallel 5
# 最多5个任务并发执行
```

### 5.2 结果对比（历史趋势）
**优化**: 保存历史结果，支持文本形式的趋势对比

```bash
$ python main.py --task operation_log --compare-with last
# 与上次稽核结果对比
```

```
差异对比:
+---------------------+------------+------------+----------+
| 检查项              | 上次异常数 | 本次异常数 | 变化     |
+---------------------+------------+------------+----------+
| account_id_null     |  1,234,567 |  1,198,234 | ↓36,333  |
| src_ip_null         |     45,678 |     48,901 | ↑3,223   | <- 需关注
+---------------------+------------+------------+----------+
```

### 5.3 快捷结果查看
**优化**: 执行完成后在终端直接展示关键摘要

```bash
$ python main.py --task operation_log --quick-view
# 执行完成后，直接在终端输出摘要，无需打开文件
```

---

## 六、实施优先级

根据"使用体验提升最大化"原则，建议按以下顺序实施：

| 阶段 | 优化项 | 预计收益 |
|------|--------|----------|
| **第一优先级** | 配置生成向导 (--init-task) | 大幅降低新建任务门槛 |
| | 配置验证 (--validate) | 提前发现配置错误，减少调试 |
| | ANSI颜色高亮 + 结构化表格 | 报告可读性提升 |
| **第二优先级** | 交互式任务选择器 | 无需记任务名，使用更直觉 |
| | 任务预览模式 (--preview) | 执行前心里有数 |
| | 进度实时显示 | 大任务执行体验提升 |
| **第三优先级** | 并发执行 (--parallel) | 多任务执行速度提升 |
| | 结果对比 (--compare) | 支持历史趋势分析 |
| | 配置继承与模板复用 | 减少重复配置工作量 |
