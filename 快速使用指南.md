# 数据质量稽核工具 - 快速使用指南

## 一、快速开始

### 1. 配置数据库连接和时间范围

编辑 `config/config.yaml`：

```yaml
# 数据库连接配置
database_host: "10.10.26.93"
database_port: 8123
database_name: "argus"
database_user: "default"
database_password: "your_password"

# 时间范围配置
start_time: "2026-01-12 00:00:00"
end_time: "2026-01-13 00:00:00"
```

### 2. 执行稽核任务

```bash
# 执行单个稽核任务（参数值为 config/tasks/ 目录下的 YAML 文件名，不含 .yaml 后缀）
python main.py --task operation_log    # 读取 config/tasks/operation_log.yaml
python main.py --task gold_log         # 读取 config/tasks/gold_log.yaml

# 执行所有稽核任务
python main.py --all-tasks

# 生成大SQL文件（不执行稽核）
python main.py --gen-sql --task operation_log
```

**重要说明**：
- `--task` 参数的值是 `config/tasks/` 目录下的 YAML 文件名（不含 `.yaml` 后缀）
- 例如：`--task gold_log` 会读取 `config/tasks/gold_log.yaml` 文件
- 文件名即为稽核任务的标识符
- 为了向后兼容，`--table` 参数仍然可用（等同于 `--task`）

### 3. 查看结果

- **稽核报告**: `output/` 目录下的 `.txt` 文件
- **日志文件**: `logs/` 目录下的 `.log` 文件
- **生成的SQL**: `big_sql/` 目录下的 `.sql` 文件

## 二、核心概念

### 稽核任务（Audit Task）

系统采用**任务化配置**，每个稽核任务对应一个配置文件：

```
config/tasks/
├── operation_log.yaml        # 4A操作日志表稽核（43个子任务）
├── gold_log.yaml             # 4A金库审批日志表稽核（26个子任务）
└── special_statistics.yaml   # 特殊统计稽核（1个子任务）
```

每个稽核任务包含：
- **任务名称**: 显示在日志中的友好名称
- **表信息**: 数据库表配置
- **子任务列表**: 具体的检查项（非空检查、枚举值检查等）

### 子任务（Sub-task）

每个子任务对应一个具体的数据质量检查，例如：
- 非空检查（null_check）
- 枚举值检查（enum_check）
- 唯一性检查（uniqueness_check）
- 语义检查（semantic_check）
- 条件非空检查（conditional_null_check）

## 三、配置文件说明

### 任务配置文件结构

```yaml
# 稽核任务名称（第一级配置）
task_name: "4A操作日志表稽核"

# 表信息配置
table_info:
  name: "operation_log"
  display_name: "4A操作日志表"
  standard_table: "argus.bg_4a_operation_log_standard"
  error_table: "argus.bg_4a_operation_log_error"
  time_field: "generic_into_time"
  template_dir: "operation_log"

# 子任务列表
tasks:
  - task_id: "log_id_null"
    task_name: "日志ID非空检查"
    enabled: true
    sql_template: "operation_log_null_check"
    params:
      field: "log_id"
      display_name: "日志ID"
```

### 配置项说明

| 配置项 | 说明 | 示例 |
|--------|------|------|
| `task_name` | 稽核任务名称（第一级） | "4A操作日志表稽核" |
| `table_info.name` | 表标识符 | "operation_log" |
| `table_info.display_name` | 表显示名称 | "4A操作日志表" |
| `table_info.standard_table` | 标准表名 | "argus.bg_4a_operation_log_standard" |
| `table_info.error_table` | 错误表名 | "argus.bg_4a_operation_log_error" |
| `table_info.time_field` | 时间字段名 | "generic_into_time" |
| `table_info.template_dir` | SQL模板目录 | "operation_log" |
| `tasks[].task_id` | 子任务ID | "log_id_null" |
| `tasks[].task_name` | 子任务名称 | "日志ID非空检查" |
| `tasks[].enabled` | 是否启用 | true/false |
| `tasks[].sql_template` | SQL模板名称 | "operation_log_null_check" |
| `tasks[].params` | 自定义参数 | field, display_name等 |

## 四、使用场景

### 场景1：日常稽核

```bash
# 每天定时执行所有稽核任务
python main.py --all-tasks
```

### 场景2：单任务稽核

```bash
# 只稽核操作日志
python main.py --task operation_log
```

### 场景3：生成SQL用于手动执行

```bash
# 生成SQL文件，可以在ClickHouse中手动执行
python main.py --gen-sql --task operation_log

# 查看生成的SQL
cat big_sql/operation_log_generated.sql
```

### 场景4：修改时间范围

编辑 `config/config.yaml`，修改 `start_time` 和 `end_time`：

```yaml
start_time: "2026-01-01 00:00:00"
end_time: "2026-01-02 00:00:00"
```

## 五、添加新的稽核任务

### 步骤1：创建任务配置文件

在 `config/tasks/` 目录创建 `my_table.yaml`（文件名即为稽核任务标识符）：

```yaml
# 稽核任务名称（第一级配置）
task_name: "我的表稽核"

# 表信息配置
table_info:
  name: "my_table"
  display_name: "我的表"
  standard_table: "argus.my_table_standard"
  error_table: "argus.my_table_error"
  time_field: "generic_into_time"
  template_dir: "my_table"

# 子任务列表
tasks:
  - task_id: "field1_null"
    task_name: "字段1非空检查"
    enabled: true
    sql_template: "my_table_null_check"
    params:
      field: "field1"
      display_name: "字段1"
```

### 步骤2：创建SQL模板

在 `sql_templates/my_table/` 目录创建SQL模板：

- `my_table_null_check_stats.sql` - 统计查询
- `my_table_null_check_sample.sql` - 样例查询

### 步骤3：执行稽核

```bash
# 使用 YAML 文件名（不含 .yaml 后缀）作为参数
python main.py --task my_table
```

**说明**：
- 文件名 `my_table.yaml` 中的 `my_table` 就是稽核任务的标识符
- 执行时使用 `--task my_table` 参数
- 系统会自动查找 `config/tasks/my_table.yaml` 文件

## 六、自定义参数

系统支持在任务配置中自定义参数，这些参数会自动传递给SQL模板。

### 示例：条件非空检查

```yaml
tasks:
  - task_id: "protocol_conditional_null"
    task_name: "协议条件非空检查"
    enabled: true
    sql_template: "operation_log_conditional_null_check"
    params:
      field: "protocol"
      display_name: "协议"
      condition: "dst_device_type != 'unknown'"  # 自定义条件参数
```

SQL模板中使用 `{condition}` 占位符：

```sql
SELECT COUNT(*) AS null_count
FROM {table_standard}
WHERE {field} IS NULL 
  AND ({condition})  -- 使用自定义参数
```

详细说明请参考：[自定义参数配置说明.md](./自定义参数配置说明.md)

## 七、输出说明

### 稽核报告格式

```
============================================================
数据质量稽核报告
============================================================
稽核任务: 4A操作日志表稽核
生成时间: 2026-01-21 20:13:25
时间范围: 2026-01-12 00:00:00 ~ 2026-01-13 00:00:00

执行汇总:
  总任务数: 43
  成功: 43
  失败: 0
  成功率: 100.00%

------------------------------------------------------------
任务1: 日志ID唯一性检查 (log_id_uniqueness)
------------------------------------------------------------
统计结果:
  dst_device_type  total_records  duplicate_records  duplicate_percentage
  database         24             24                 100.0000
  host             4              0                  0.0000

样例数据:
  log_id  dst_device_type
  ...
```

### 日志输出示例

```
[2026-01-21 20:12:17] [INFO] 总体稽核任务启动
[2026-01-21 20:12:17] [INFO] 发现 3 个稽核任务配置:
[2026-01-21 20:12:17] [INFO]   - 4A金库审批日志表稽核 (26 个子任务)
[2026-01-21 20:12:17] [INFO]   - 4A操作日志表稽核 (43 个子任务)
[2026-01-21 20:12:17] [INFO]   - 特殊统计稽核 (1 个子任务)
[2026-01-21 20:12:17] [INFO] 
[2026-01-21 20:12:17] [INFO] 总体稽核任务统计:
[2026-01-21 20:12:17] [INFO]   稽核任务数量: 3
[2026-01-21 20:12:17] [INFO]   子任务总数: 70
[2026-01-21 20:12:17] [INFO] 
[2026-01-21 20:12:17] [INFO] [1/3] 执行稽核任务: 4A金库审批日志表稽核
[2026-01-21 20:12:17] [INFO] 稽核任务: 4A金库审批日志表稽核
[2026-01-21 20:12:17] [INFO] 共加载 26 个子任务
[2026-01-21 20:12:17] [INFO] 开始执行任务: gold_grant_result_uniqueness - 金库授权结果唯一性检查
[2026-01-21 20:12:17] [INFO] 查询成功: gold_log_uniqueness_check_stats (行数: 1, 耗时: 0.16s)
[2026-01-21 20:12:18] [INFO] 任务完成: gold_grant_result_uniqueness
```

## 八、常见问题

### Q1: 如何修改稽核时间范围？

编辑 `config/config.yaml`，修改 `start_time` 和 `end_time`。

### Q2: 如何禁用某个子任务？

在任务配置文件中，将子任务的 `enabled` 设置为 `false`：

```yaml
tasks:
  - task_id: "log_id_null"
    task_name: "日志ID非空检查"
    enabled: false  # 禁用此任务
```

### Q3: 如何查看SQL执行详情？

查看 `logs/` 目录下的日志文件，包含每个SQL的执行时间和结果。

### Q4: --task 参数应该填什么？

`--task` 参数的值是 `config/tasks/` 目录下的 YAML 文件名（不含 `.yaml` 后缀）。

例如：
- `config/tasks/operation_log.yaml` → `--task operation_log`
- `config/tasks/gold_log.yaml` → `--task gold_log`
- `config/tasks/my_custom_task.yaml` → `--task my_custom_task`

**向后兼容**：`--table` 参数仍然可用，等同于 `--task`。

### Q5: 如何添加新的检查类型？

### Q5: 如何添加新的检查类型？

1. 创建SQL模板（_stats.sql 和 _sample.sql）
2. 在任务配置中添加子任务
3. 指定 `sql_template` 为新模板名称

### Q6: 支持哪些参数类型？

### Q6: 支持哪些参数类型？

系统自动提供：
- `start_time`, `end_time` - 时间范围
- `table_standard`, `table_error` - 表名
- `time_field` - 时间字段

自定义参数：
- `field` - 字段名
- `display_name` - 显示名称
- `condition` - 条件表达式
- 任意其他参数

### Q7: 为什么某些稽核任务没有样例数据？

可能的原因：
1. **时间范围内无数据**：检查 `config.yaml` 中的 `start_time` 和 `end_time` 是否正确
2. **数据全部正常**：如果统计结果显示无异常，则不会有样例数据
3. **查询条件过滤**：某些表有特殊过滤条件（如金库日志的 `standby1 IS NULL`）

解决方法：
- 调整时间范围到有数据的时间段
- 查看统计结果中的 `total_records` 是否为 0
- 检查表配置中的 `filter_condition` 是否过滤了所有数据

## 九、最佳实践

### 1. 命名规范

- 任务ID使用下划线命名：`log_id_null`
- 任务名称使用中文：`日志ID非空检查`
- SQL模板使用表名前缀：`operation_log_null_check`

### 2. 性能优化

- 合理设置时间范围，避免查询过大数据量
- 使用索引字段作为查询条件
- 定期清理旧的日志和报告文件

### 3. 错误处理

- 查看日志文件定位问题
- 使用 `--gen-sql` 生成SQL手动测试
- 检查数据库连接和权限

### 4. 定时执行

使用系统定时任务（cron/Task Scheduler）：

```bash
# Linux cron示例（每天凌晨2点执行）
0 2 * * * cd /path/to/data-quality-audit-py && python main.py --all-tasks

# Windows Task Scheduler
# 创建计划任务，执行: python main.py --all-tasks
```

## 十、相关文档

- [自定义参数配置说明](./自定义参数配置说明.md) - 详细的参数配置指南
- [自定义SQL模板使用指南](./自定义SQL模板使用指南.md) - SQL模板开发指南
- [生产环境部署指南](./生产环境部署指南.md) - 生产环境部署说明
- [完整测试报告](./完整测试报告_20260121.md) - 最新的测试结果

## 十一、技术支持

如有问题，请查看：
1. 日志文件：`logs/audit_*.log`
2. 配置文件：`config/config.yaml`
3. 任务配置：`config/tasks/*.yaml`
4. SQL模板：`sql_templates/*/`
