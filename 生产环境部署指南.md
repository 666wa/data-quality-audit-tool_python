# 数据质量稽核工具 - 生产环境部署指南

## 环境要求

- **操作系统**: Linux (CentOS 7+, Ubuntu 18.04+, 或其他主流发行版)
- **Python版本**: Python 3.7+
- **依赖包**: requests, PyYAML (已包含在离线包中)

## 部署步骤

### 方式一：在线部署（推荐用于有网络的环境）

1. **上传项目文件**
   ```bash
   # 将整个 data-quality-audit-py 目录上传到服务器
   scp -r data-quality-audit-py user@server:/path/to/
   ```

2. **进入项目目录**
   ```bash
   cd /path/to/data-quality-audit-py
   ```

3. **运行部署脚本**
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   ```

4. **配置数据库连接**
   ```bash
   vi config/config.yaml
   # 修改数据库连接信息
   ```

5. **运行稽核**
   ```bash
   ./run.sh
   # 或
   python3 main.py
   ```

### 方式二：离线部署（推荐用于无网络的生产环境）

#### 步骤1：在有网络的环境准备离线包

1. **下载离线依赖**
   ```bash
   cd data-quality-audit-py
   python3 download_packages.py
   ```
   这会在 `offline_packages/` 目录下载所有依赖的 wheel 文件。

2. **打包整个项目**
   ```bash
   cd ..
   zip -r data-quality-audit-py.zip data-quality-audit-py/ \
       -x "data-quality-audit-py/logs/*" \
       -x "data-quality-audit-py/output/*" \
       -x "data-quality-audit-py/__pycache__/*" \
       -x "data-quality-audit-py/src/__pycache__/*" \
       -x "data-quality-audit-py/venv/*"
   ```

#### 步骤2：在生产环境部署

1. **上传压缩包**
   ```bash
   scp data-quality-audit-py.zip user@server:/path/to/
   ```

2. **解压**
   ```bash
   cd /path/to/
   unzip data-quality-audit-py.zip
   cd data-quality-audit-py
   ```

3. **运行部署脚本**
   ```bash
   chmod +x deploy.sh
   ./deploy.sh
   # 选择创建虚拟环境: y
   # 脚本会自动使用 offline_packages 中的离线包
   ```

4. **配置数据库连接**
   ```bash
   vi config/config.yaml
   ```

5. **运行稽核**
   ```bash
   ./run.sh
   ```

## 配置说明

### 1. 数据库配置 (config/config.yaml)

```yaml
database:
  host: "10.10.26.93"      # ClickHouse地址
  port: 8123                # HTTP端口
  user: "default"           # 用户名
  password: ""              # 密码

table_standard: "argus.bg_4a_operation_log_standard"
table_error: "argus.bg_4a_operation_log_error"
time_field: "generic_into_time"

time_range:
  start_time: "2024-12-16 00:00:00"
  end_time: "2024-12-16 23:59:59"
```

### 2. 稽核任务配置 (config/audit_tasks.yaml)

根据实际需求配置要稽核的字段和检查类型。

## 使用方式

### 执行完整稽核
```bash
./run.sh
# 或
python3 main.py
```

### 生成大SQL文件
```bash
./run.sh --gen-sql
# 或
python3 main.py --gen-sql
```

### 查看帮助
```bash
python3 main.py --help
```

## 输出文件

- **稽核报告**: `output/audit_report_YYYYMMDD_HHMMSS.txt`
- **日志文件**: `logs/audit_YYYYMMDD_HHMMSS.log`
- **大SQL文件**: `big_sql/generated.sql`

## 定时任务配置

### 使用 crontab 设置定时执行

1. **编辑 crontab**
   ```bash
   crontab -e
   ```

2. **添加定时任务**
   ```bash
   # 每天凌晨2点执行稽核
   0 2 * * * cd /path/to/data-quality-audit-py && ./run.sh >> /path/to/cron.log 2>&1
   
   # 每周一上午9点执行稽核
   0 9 * * 1 cd /path/to/data-quality-audit-py && ./run.sh >> /path/to/cron.log 2>&1
   ```

### 使用 systemd 定时器（推荐）

1. **创建服务文件**
   ```bash
   sudo vi /etc/systemd/system/data-audit.service
   ```
   
   内容：
   ```ini
   [Unit]
   Description=Data Quality Audit Service
   
   [Service]
   Type=oneshot
   User=your_user
   WorkingDirectory=/path/to/data-quality-audit-py
   ExecStart=/path/to/data-quality-audit-py/run.sh
   ```

2. **创建定时器文件**
   ```bash
   sudo vi /etc/systemd/system/data-audit.timer
   ```
   
   内容：
   ```ini
   [Unit]
   Description=Data Quality Audit Timer
   
   [Timer]
   OnCalendar=daily
   OnCalendar=02:00
   Persistent=true
   
   [Install]
   WantedBy=timers.target
   ```

3. **启用定时器**
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable data-audit.timer
   sudo systemctl start data-audit.timer
   ```

## 故障排查

### 1. Python版本问题
```bash
# 检查Python版本
python3 --version

# 如果版本过低，需要升级Python
```

### 2. 依赖安装失败
```bash
# 手动安装依赖
pip3 install requests PyYAML

# 或使用离线包
pip3 install --no-index --find-links=offline_packages -r requirements.txt
```

### 3. 数据库连接失败
- 检查 `config/config.yaml` 中的数据库配置
- 确认网络连通性：`telnet 10.10.26.93 8123`
- 查看日志文件：`tail -f logs/audit_*.log`

### 4. 权限问题
```bash
# 确保脚本有执行权限
chmod +x deploy.sh run.sh main.py

# 确保目录有写权限
chmod -R 755 logs output big_sql
```

## 性能优化建议

1. **时间范围**: 不要设置过大的时间范围，建议按天或按周执行
2. **并发控制**: 如果数据量大，可以考虑分批执行
3. **资源监控**: 监控服务器CPU和内存使用情况

## 安全建议

1. **配置文件权限**: 
   ```bash
   chmod 600 config/config.yaml  # 保护数据库密码
   ```

2. **日志轮转**: 定期清理旧日志
   ```bash
   # 删除30天前的日志
   find logs/ -name "*.log" -mtime +30 -delete
   ```

3. **报告归档**: 定期归档或删除旧报告
   ```bash
   # 压缩30天前的报告
   find output/ -name "*.txt" -mtime +30 -exec gzip {} \;
   ```

## 联系支持

如有问题，请查看：
- 日志文件：`logs/audit_*.log`
- 文档：`README.md`
- 快速指南：`快速使用指南.md`
