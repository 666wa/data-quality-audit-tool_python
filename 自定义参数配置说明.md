# 自定义参数配置说明

## 功能概述

✅ **已实现功能**：可以在 `tasks.yaml` 文件中自定义配置参数，这些参数会自动传递给SQL模板使用。

## 工作原理

### 1. 参数配置位置
在 `config/tasks/` 目录下的任务配置文件中（如 `operation_log.yaml`），每个任务都可以配置自定义参数：

```yaml
tasks:
  - task_id: "log_id_null"
    task_name: "日志ID非空检查"
    enabled: true
    sql_template: "operation_log_null_check"
    params:
      field: "log_id"              # 自定义参数1
      display_name: "日志ID"       # 自定义参数2
```

### 2. 参数传递流程

```
tasks.yaml 配置参数
    ↓
audit_executor.py 读取并合并参数
    ↓
SQL模板使用 {参数名} 占位符
    ↓
自动替换为实际值
```

### 3. SQL模板中使用参数

在SQL模板文件中，使用 `{参数名}` 格式的占位符：

```sql
-- 示例：operation_log_null_check_stats.sql
SELECT 
    dst_device_type,
    COUNT(*) AS total_count,
    SUM(CASE WHEN {field} IS NULL OR {field} = '' THEN 1 ELSE 0 END) AS null_count
FROM {table_standard}
WHERE {time_field} >= '{start_time}' 
  AND {time_field} < '{end_time}'
GROUP BY dst_device_type;
```

## 可用参数类型

### 系统自动提供的参数
这些参数由系统自动提供，无需在 `tasks.yaml` 中配置：

| 参数名 | 说明 | 来源 |
|--------|------|------|
| `start_time` | 稽核开始时间 | `config.yaml` |
| `end_time` | 稽核结束时间 | `config.yaml` |
| `table_standard` | 标准表名 | `tasks.yaml` 的 `table_info` |
| `table_error` | 错误表名 | `tasks.yaml` 的 `table_info` |
| `time_field` | 时间字段名 | `tasks.yaml` 的 `table_info` |

### 任务自定义参数
在 `tasks.yaml` 的 `params` 中配置，可以是任意参数：

| 参数名 | 说明 | 示例值 |
|--------|------|--------|
| `field` | 要检查的字段名 | `"log_id"` |
| `display_name` | 字段显示名称 | `"日志ID"` |
| `condition` | 条件表达式 | `"dst_device_type != 'unknown'"` |
| **任意自定义参数** | 你可以添加任何参数 | 任意值 |

## 使用示例

### 示例1：基础参数使用

**任务配置** (`operation_log.yaml`):
```yaml
tasks:
  - task_id: "log_id_null"
    task_name: "日志ID非空检查"
    enabled: true
    sql_template: "operation_log_null_check"
    params:
      field: "log_id"
      display_name: "日志ID"
```

**SQL模板** (`operation_log_null_check_stats.sql`):
```sql
SELECT 
    dst_device_type,
    COUNT(*) AS total_count,
    SUM(CASE WHEN {field} IS NULL THEN 1 ELSE 0 END) AS null_count
FROM {table_standard}
WHERE {time_field} >= '{start_time}' 
  AND {time_field} < '{end_time}'
GROUP BY dst_device_type;
```

**实际执行的SQL**:
```sql
SELECT 
    dst_device_type,
    COUNT(*) AS total_count,
    SUM(CASE WHEN log_id IS NULL THEN 1 ELSE 0 END) AS null_count
FROM argus.bg_4a_operation_log_standard
WHERE generic_into_time >= '2026-01-12 00:00:00' 
  AND generic_into_time < '2026-01-13 00:00:00'
GROUP BY dst_device_type;
```

### 示例2：条件参数使用

**任务配置**:
```yaml
tasks:
  - task_id: "person_liable_conditional_null"
    task_name: "责任人条件非空检查"
    enabled: true
    sql_template: "operation_log_conditional_null_check"
    params:
      field: "person_liable"
      display_name: "责任人"
      condition: "dst_device_type != 'unknown'"
```

**SQL模板** (`operation_log_conditional_null_check_stats.sql`):
```sql
SELECT 
    dst_device_type,
    COUNT(*) AS total_count,
    SUM(CASE WHEN {field} IS NULL AND ({condition}) THEN 1 ELSE 0 END) AS null_count
FROM {table_standard}
WHERE {time_field} >= '{start_time}' 
  AND {time_field} < '{end_time}'
GROUP BY dst_device_type;
```

**实际执行的SQL**:
```sql
SELECT 
    dst_device_type,
    COUNT(*) AS total_count,
    SUM(CASE WHEN person_liable IS NULL AND (dst_device_type != 'unknown') THEN 1 ELSE 0 END) AS null_count
FROM argus.bg_4a_operation_log_standard
WHERE generic_into_time >= '2026-01-12 00:00:00' 
  AND generic_into_time < '2026-01-13 00:00:00'
GROUP BY dst_device_type;
```

### 示例3：自定义参数（高级用法）

你可以添加任意自定义参数：

**任务配置**:
```yaml
tasks:
  - task_id: "custom_threshold_check"
    task_name: "自定义阈值检查"
    enabled: true
    sql_template: "custom_threshold_check"
    params:
      field: "response_time"
      display_name: "响应时间"
      threshold: "1000"              # 自定义参数：阈值
      unit: "ms"                     # 自定义参数：单位
      min_value: "0"                 # 自定义参数：最小值
      max_value: "10000"             # 自定义参数：最大值
```

**SQL模板** (`custom_threshold_check_stats.sql`):
```sql
SELECT 
    dst_device_type,
    COUNT(*) AS total_count,
    SUM(CASE WHEN {field} > {threshold} THEN 1 ELSE 0 END) AS exceed_count,
    AVG({field}) AS avg_value,
    MIN({field}) AS min_value,
    MAX({field}) AS max_value
FROM {table_standard}
WHERE {time_field} >= '{start_time}' 
  AND {time_field} < '{end_time}'
  AND {field} >= {min_value}
  AND {field} <= {max_value}
GROUP BY dst_device_type;
```

## 参数合并规则

系统会按以下顺序合并参数（后面的会覆盖前面的）：

1. **系统默认参数**：`start_time`, `end_time`
2. **表配置参数**：`table_standard`, `table_error`, `time_field`
3. **任务自定义参数**：`params` 中配置的所有参数

代码实现（`audit_executor.py`）：
```python
# 准备参数
exec_params = {
    'start_time': self.config.get('start_time'),
    'end_time': self.config.get('end_time')
}

# 添加表配置参数
if self.table_config and 'table_info' in self.table_config:
    table_info = self.table_config['table_info']
    exec_params['table_standard'] = table_info.get('standard_table')
    exec_params['table_error'] = table_info.get('error_table')
    exec_params['time_field'] = table_info.get('time_field')

# 添加任务参数（会覆盖前面的同名参数）
exec_params.update(params)
```

## 实际应用场景

### 场景1：不同字段使用相同模板

```yaml
tasks:
  # 使用相同的 null_check 模板检查不同字段
  - task_id: "log_id_null"
    sql_template: "operation_log_null_check"
    params:
      field: "log_id"
      display_name: "日志ID"
  
  - task_id: "session_id_null"
    sql_template: "operation_log_null_check"
    params:
      field: "session_id"
      display_name: "会话ID"
  
  - task_id: "account_id_null"
    sql_template: "operation_log_null_check"
    params:
      field: "account_id"
      display_name: "账号ID"
```

### 场景2：条件检查

```yaml
tasks:
  # 只在特定条件下检查非空
  - task_id: "protocol_conditional_null"
    sql_template: "operation_log_conditional_null_check"
    params:
      field: "protocol"
      display_name: "协议"
      condition: "dst_device_type != 'unknown'"
  
  - task_id: "from_account_conditional_null"
    sql_template: "operation_log_conditional_null_check"
    params:
      field: "from_account"
      display_name: "来源账号"
      condition: "operate_source = 'remote'"
```

### 场景3：枚举值检查

```yaml
tasks:
  - task_id: "log_source_enum"
    sql_template: "operation_log_enum_check"
    params:
      field: "log_source"
      display_name: "日志来源"
      # 可以添加期望的枚举值列表
      expected_values: "('A', 'B', 'C')"
```

## 添加新的自定义检查

### 步骤1：创建SQL模板

创建 `sql_templates/my_table/my_custom_check_stats.sql`:
```sql
-- 自定义检查 - 统计版本
SELECT 
    {group_by_field},
    COUNT(*) AS total_count,
    SUM(CASE WHEN {check_condition} THEN 1 ELSE 0 END) AS issue_count,
    round(issue_count * 100.0 / total_count, 4) AS issue_percentage
FROM {table_standard}
WHERE {time_field} >= '{start_time}' 
  AND {time_field} < '{end_time}'
GROUP BY {group_by_field}
ORDER BY issue_percentage DESC;
```

创建 `sql_templates/my_table/my_custom_check_sample.sql`:
```sql
-- 自定义检查 - 样例版本
SELECT 
    log_id,
    {field},
    {group_by_field}
FROM {table_standard}
WHERE {time_field} >= '{start_time}' 
  AND {time_field} < '{end_time}'
  AND {check_condition}
LIMIT 10;
```

### 步骤2：配置任务

在 `config/tasks/my_table.yaml` 中添加：
```yaml
tasks:
  - task_id: "my_custom_check"
    task_name: "我的自定义检查"
    enabled: true
    sql_template: "my_custom_check"
    params:
      field: "my_field"
      display_name: "我的字段"
      group_by_field: "dst_device_type"
      check_condition: "my_field > 100 AND my_field < 1000"
```

### 步骤3：执行稽核

```bash
python main.py --table my_table
```

## 注意事项

### 1. 参数命名规范
- 使用小写字母和下划线
- 避免使用SQL关键字
- 建议使用有意义的名称

### 2. 参数值类型
- 字符串值：会直接替换，注意SQL注入风险
- 数值：可以直接使用
- 条件表达式：确保SQL语法正确

### 3. SQL注入防护
虽然系统使用简单的字符串替换，但由于参数来自配置文件（不是用户输入），风险较低。建议：
- 不要在参数中使用用户输入的值
- 验证配置文件的来源
- 定期审查配置文件内容

### 4. 调试技巧
如果SQL执行失败，可以：
1. 查看日志文件 `logs/audit_*.log`
2. 使用 `--gen-sql` 生成SQL文件查看实际SQL
3. 在ClickHouse中手动执行SQL测试

```bash
# 生成SQL文件查看实际执行的SQL
python main.py --gen-sql --table operation_log

# 查看生成的SQL
cat big_sql/operation_log_generated.sql
```

## 总结

✅ **功能已完全实现**：
- 可以在 `tasks.yaml` 中配置任意自定义参数
- 参数会自动传递给SQL模板
- 使用 `{参数名}` 占位符在SQL中引用
- 支持参数覆盖和合并

✅ **灵活性**：
- 同一个SQL模板可以复用于多个任务
- 每个任务可以有不同的参数值
- 可以添加任意数量的自定义参数

✅ **易用性**：
- 配置简单，只需在YAML中添加参数
- 不需要修改代码
- 支持条件检查、阈值检查等各种场景
