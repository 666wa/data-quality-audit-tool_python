# 配置文件精简完成

## 完成时间
2026-01-21

## 重构目标

精简 `config.yaml` 配置文件，移除操作日志表的特定配置，只保留核心的数据库连接和目录配置，使配置更加通用和清晰。

## 重构内容

### 1. config.yaml 精简

#### 移除的配置项
```yaml
# 移除了以下操作日志表专用配置
table_standard: "argus.bg_4a_operation_log_standard"
table_error: "argus.bg_4a_operation_log_error"
time_field: "generic_into_time"
queries_dir: "./queries"  # 未使用的配置
```

#### 保留的配置项
```yaml
# 数据库连接配置（必需）
database_host: "10.10.26.93"
database_port: 8123
database_name: "argus"
database_user: "default"
database_password: "ugOZymrL54TawiV4"

# 时间范围配置（必需）
start_time: "2026-01-12 00:00:00"
end_time: "2026-01-19 00:00:00"

# 目录配置（可选，有默认值）
tasks_dir: "./config/tasks"               # 任务配置目录
sql_templates_dir: "./sql_templates"      # SQL模板根目录
report_output_dir: "./output"             # 报告输出目录
log_output_dir: "./logs"                  # 日志输出目录
```

### 2. 配置结构优化

#### 精简后的配置结构
```
config.yaml
├── 数据库连接配置（5项）
├── 时间范围配置（2项）
└── 目录配置（4项）
```

**总计**: 11个配置项，全部都是实际使用的配置

#### 配置说明
- **数据库连接配置**: 连接ClickHouse数据库所需的参数
- **时间范围配置**: 稽核的时间范围
- **目录配置**: 各种文件的存放目录，都可以自定义

### 3. 代码修改

#### main.py 修改

**修改1: 必须指定表名**
```python
# 旧的方式（已移除）
python main.py                    # 使用默认配置

# 新的方式（必需）
python main.py --table operation_log    # 必须指定表名
python main.py --all-tables             # 或稽核所有表
```

**修改2: generate_big_sql 函数**
```python
# 旧的签名
def generate_big_sql(config_manager, logger, table_name=None):

# 新的签名
def generate_big_sql(config_manager, logger, table_name):
```
- 移除了 `table_name=None` 的默认值
- 移除了对旧配置的支持代码
- 必须传入表名参数

**修改3: run_audit 函数**
```python
# 旧的签名
def run_audit(config_manager, logger, table_name=None):

# 新的签名
def run_audit(config_manager, logger, table_name):
```
- 移除了 `table_name=None` 的默认值
- 移除了对旧配置的支持代码
- 必须传入表名参数

**修改4: main 函数**
```python
# 新增参数验证
if args.gen_sql:
    if not args.table:
        logger.error("生成大SQL需要指定表名，请使用 --table 参数")
        return 1

# 新增默认行为提示
else:
    logger.error("请指定要稽核的表名或使用 --all-tables 参数")
    logger.info("使用示例:")
    logger.info("  python main.py --table operation_log")
    return 1
```

### 4. 使用方式变更

#### 旧的使用方式（已废弃）
```bash
# ❌ 不再支持
python main.py                    # 使用config.yaml中的table_*配置
python main.py --gen-sql          # 使用config.yaml中的table_*配置
```

#### 新的使用方式（推荐）
```bash
# ✅ 稽核指定表
python main.py --table operation_log
python main.py --table gold_log

# ✅ 生成大SQL
python main.py --gen-sql --table operation_log
python main.py --gen-sql --table gold_log

# ✅ 稽核所有表
python main.py --all-tables
```

### 5. 配置文件对比

#### 精简前（旧版本）
```yaml
# 数据库连接配置（5项）
database_host: "..."
database_port: 8123
database_name: "..."
database_user: "..."
database_password: "..."

# 时间范围配置（2项）
start_time: "..."
end_time: "..."

# 操作日志表配置（3项）- 已移除
table_standard: "..."
table_error: "..."
time_field: "..."

# 目录配置（5项）
queries_dir: "./queries"          # 未使用，已移除
report_output_dir: "./output"
log_output_dir: "./logs"
tasks_dir: "./config/tasks"
sql_templates_dir: "./sql_templates"
```
**总计**: 15个配置项

#### 精简后（新版本）
```yaml
# 数据库连接配置（5项）
database_host: "..."
database_port: 8123
database_name: "..."
database_user: "..."
database_password: "..."

# 时间范围配置（2项）
start_time: "..."
end_time: "..."

# 目录配置（4项）
tasks_dir: "./config/tasks"
sql_templates_dir: "./sql_templates"
report_output_dir: "./output"
log_output_dir: "./logs"
```
**总计**: 11个配置项

**减少**: 4个配置项（-27%）

## 测试验证

### 测试1: 不带参数执行
```bash
$ python main.py
```

输出：
```
[2026-01-21 19:49:10] [ERROR] 请指定要稽核的表名或使用 --all-tables 参数
[2026-01-21 19:49:10] [INFO] 使用示例:
[2026-01-21 19:49:10] [INFO]   python main.py --table operation_log
[2026-01-21 19:49:10] [INFO]   python main.py --table gold_log
[2026-01-21 19:49:10] [INFO]   python main.py --all-tables
```

✅ 成功：提示用户必须指定表名

### 测试2: 带表名参数执行
```bash
$ python main.py --table operation_log
```

输出：
```
[2026-01-21 19:49:30] [INFO] 数据质量稽核工具启动
[2026-01-21 19:49:30] [INFO] 加载配置文件...
[2026-01-21 19:49:30] [INFO] 使用表配置: operation_log
[2026-01-21 19:49:30] [INFO] 加载表配置: config\tasks\operation_log.yaml
[2026-01-21 19:49:30] [INFO] 成功加载表配置: 4A操作日志表
[2026-01-21 19:49:31] [INFO] 使用SQL模板目录: sql_templates\operation_log
[2026-01-21 19:49:31] [INFO] 开始执行任务: log_id_uniqueness - 日志ID唯一性检查
```

✅ 成功：正常执行稽核任务

### 测试3: 生成大SQL不带表名
```bash
$ python main.py --gen-sql
```

输出：
```
[ERROR] 生成大SQL需要指定表名，请使用 --table 参数
[INFO] 示例: python main.py --gen-sql --table operation_log
```

✅ 成功：提示用户必须指定表名

### 测试4: 生成大SQL带表名
```bash
$ python main.py --gen-sql --table operation_log
```

输出：
```
[INFO] 使用任务化配置生成SQL
[INFO] 生成统计查询: 43 条
[INFO] 生成样例查询: 43 条
[INFO] 总计: 87 条SQL语句
[INFO] SQL文件已生成: big_sql\operation_log_generated.sql
```

✅ 成功：正常生成大SQL文件

## 优势

### 1. 配置更清晰
- 移除了特定表的配置，使配置文件更通用
- 所有表的配置都在 `tasks_dir` 目录中，结构统一
- 配置项减少27%，更易理解和维护

### 2. 使用更规范
- 必须明确指定要稽核的表，避免误操作
- 使用方式统一，所有表都用相同的命令格式
- 错误提示清晰，帮助用户正确使用

### 3. 扩展更方便
- 添加新表只需在 `tasks_dir` 创建配置文件
- 不需要修改 `config.yaml`
- 所有表配置独立，互不影响

### 4. 维护更简单
- 配置文件职责单一，只管理全局配置
- 表配置和全局配置分离，便于管理
- 减少了配置冗余和混淆

## 配置文件职责划分

### config.yaml（全局配置）
- ✅ 数据库连接信息
- ✅ 时间范围配置
- ✅ 目录路径配置

### config/tasks/*.yaml（表配置）
- ✅ 表名和字段信息
- ✅ 稽核任务定义
- ✅ SQL模板目录

### sql_templates/*/（SQL模板）
- ✅ 具体的SQL查询逻辑

## 迁移指南

如果你之前使用旧的配置方式，需要进行以下调整：

### 步骤1: 更新 config.yaml
移除以下配置项：
```yaml
# 删除这些行
table_standard: "..."
table_error: "..."
time_field: "..."
queries_dir: "..."
```

### 步骤2: 更新命令行
```bash
# 旧命令
python main.py

# 新命令
python main.py --table operation_log
```

### 步骤3: 更新脚本
如果有自动化脚本，需要添加 `--table` 参数：
```bash
# 旧脚本
python main.py --gen-sql

# 新脚本
python main.py --gen-sql --table operation_log
```

## 总结

配置文件精简完成，主要改进：

✅ 移除了操作日志表的特定配置
✅ 配置项从15个减少到11个（-27%）
✅ 配置职责更清晰，全局配置和表配置分离
✅ 使用方式更规范，必须明确指定表名
✅ 扩展更方便，添加新表不需要修改全局配置
✅ 所有测试通过，功能正常

现在 `config.yaml` 只包含核心的数据库连接和目录配置，更加通用和易于维护。
